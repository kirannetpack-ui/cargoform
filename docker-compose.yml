services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: cargoform
      POSTGRES_USER: cargoform
      POSTGRES_PASSWORD: cargoform
    ports:
      - "5432:5432"
    volumes:
      - cargoform_postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cargoform"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  cargoform_postgres:

